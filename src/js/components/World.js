const React = require('canvas/react');
const World = React.createClass({

  divstyle: {
    padding: '50px',
  },

  render() {
    return (
      <span>World!</span>
    );
  }
});
module.exports = World;